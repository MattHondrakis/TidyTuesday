---
title: "Bee Colony"
author: "Matthew"
date: "1/11/2022"
output: 
  html_document:
    keep_md: true
    toc: true
    toc_float: true
editor_options:
  chunk_output_type: console
---

```{r setup, include=FALSE}
knitr::opts_chunk$set(echo = TRUE)
library(tidyverse)
theme_set(theme_bw())

colony <- read_csv('https://raw.githubusercontent.com/rfordatascience/tidytuesday/master/data/2022/2022-01-11/colony.csv')
stressor <- read_csv('https://raw.githubusercontent.com/rfordatascience/tidytuesday/master/data/2022/2022-01-11/stressor.csv')

colony$year <- as.numeric(colony$year)

beecolony <- colony %>%
  inner_join(stressor, by = c("year", "months", "state")) %>%
  pivot_wider(names_from = "stressor", values_from = "stress_pct")

```


# Explore

```{r}
beecolony <- na.omit(beecolony)

beecolony %>%
  select(where(is.numeric)) %>%
  gather() %>%
  ggplot(aes(value)) + geom_histogram() + facet_wrap(~key, scales = "free")

beecolony %>%
  group_by(state) %>%
  summarize(sum(colony_lost), sum(colony_added), sum(colony_added)/sum(colony_lost)) %>%
  arrange(-`sum(colony_lost)`, `sum(colony_added)`)
```


```{r}
gplot <- function(x){
  colony %>%
    filter(!is.na({{x}})) %>%
    distinct(months, year, state, {{x}}) %>%
    group_by(months, year, state) %>%
    ggplot(aes({{x}}, reorder(state, {{x}}, order = TRUE))) + geom_col() + labs(y = "")
}

gplot(colony_max)
```

```{r}
stressor %>%
  drop_na() %>%
  mutate(stressor = as.factor(stressor), stressor = fct_relevel(stressor, c("Disesases", "Unknown", "Other", 
                                                                        "Pesticides", "Other pests/parasites", 
                                                                        "Varroa mites"))) %>%
  group_by(year, stressor) %>%
  summarize(n = sum(stress_pct)) %>%
  ggplot(aes(year, n, fill = stressor)) + geom_col(position = "dodge")

```





